import Edge from '../geometry-processing-js/node/core/edge';
import Face from '../geometry-processing-js/node/core/face';

let edge = new Edge();
console.log(edge.toString());

let face = new Face();
console.log(face.toString());